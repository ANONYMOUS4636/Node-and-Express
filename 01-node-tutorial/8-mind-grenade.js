const a=5;
const b=6;

function add(a,b){
    console.log(`sum is ${a+b}`);
}

add(a,b);

//when the node exports it export the whole code wrapped in a function and when there is a executable part it will execute itleft